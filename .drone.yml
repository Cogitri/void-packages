---
kind: pipeline
name: build-pkg

clone:
  depth: 200

volumes:
- name: distdir
  host:
    path: /srv/dist/dist
- name: keysdir
  host:
    path: /var/keys

steps:
- name: prepare-build
  image: cogitri/voidlinux-ci
  pull: always
  commands:
  - common/drone/prepare.sh
  - common/drone/bootstrap.sh

- name: do-build
  image: cogitri/voidlinux-ci
  privileged: true
  environment:
    XBPS_MAKEJOBS: 6
    XBPS_ALLOW_RESTRICTED: yes
  commands:
  - common/drone/changed_templates.sh
  - common/drone/build.sh x86_64 x86_64

- name: do-deploy
  image: cogitri/voidlinux-ci
  volumes:
  - name: distdir
    path: /dist
  - name: keysdir
    path: /keys
  environment:
    SIGN_PW:
      from_secret: pkg_sign_pw
  commands:
  - cp -r hostdir/binpkgs/systemd/* /dist
  - /keys/sign-pkgs

trigger:
  branch:
  - systemd
---
kind: signature
hmac: 29a17213a4d4806428cd6f05e8160fe708175bdbb27a3e6a48f5a3aa0d159da1

...
